@model IEnumerable<WebApplication1.Models.Labs>

@{
    ViewData["Title"] = "Labs";
}

<h3>Labs</h3>
<p><a asp-controller="Labs" asp-action="Create">Create New</a></p>
<table class="table">
    <thead>
        <tr>
            <th scope="col">@Html.DisplayNameFor(model => model.Name)</th>
            <th scope="col">@Html.DisplayNameFor(model => model.Address)</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Address)</td>
                <td>
                    <a asp-controller="Labs" asp-action="Edit" asp-route-id="@item.Id">Edit</a>    |
                    <a asp-controller="Labs" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-controller="Labs" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
